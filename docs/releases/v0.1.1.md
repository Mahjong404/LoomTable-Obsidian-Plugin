# LoomTable Obsidian Plugin v0.1.1

## 发布基线

- Plugin main: `4f77824bb924bdfa2f63cf82c35237a5ee83f661`
- Server OpenAPI source: `ef0c6bd751642f4a604fe1bf88980f64e39dd992`
- Plugin version: `0.1.1`
- Obsidian minimum version: `1.11.5`

这是在既有 `v0.1.0` 之后的补丁版本。旧 `v0.1.0` Release 仍指向 `5f5db46f37efb0f146cf53d7e8f6e3d36db179fb`，不包含本发布基线。

## 本次范围

- Connection Profile 的添加/删除按钮提供清晰的中英文文案和说明，不影响 Server 数据。
- Obsidian 语言选择支持跟随 Obsidian、English 和简体中文。
- Map Leaflet 已加载瓦片可见性修复；OSM 层不再向 Leaflet 传入 `subdomains: undefined`，显式天地图/自定义子域名保持不变。
- 天地图矢量、影像、地形及注记层共用一个 Token 设置入口；旧的按图层 Secret ID 引用会安全迁移到共享绑定。
- 保留 Grid 编辑、Mutation Queue、Conflict UI、Map Provider、凭据脱敏和既有 OpenAPI 生成类型。

## 历史版本说明

本文仅记录 v0.1.1，不作为当前开发任务。当前范围与验证方式见 [P1.6](../p1.6/README.md)。本版本不承诺公共 Provider 的 SLA、长期配额或第三方账号策略。

## 自动验证

- Plugin CI 的 `pnpm check`、OpenAPI drift、生产构建和 View 相关测试通过。
- 生产安装包只包含 `main.js`、`manifest.json` 和 `styles.css`；本次最终构建 ZIP SHA-256 为 `114011A4ED31B75DE1BC492AA0662CFCF179BD0815309EECB6350DACEA405087`。

安装时下载本版本 Release 资产，并将三个文件放入 `.obsidian/plugins/loomtable/` 后启用 Plugin。
